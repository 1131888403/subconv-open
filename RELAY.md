# 订阅中转是干什么的（大白话版）

这份文档不讲技术细节，只讲**它帮你解决什么问题**。
要部署请看 [DEPLOY.md](DEPLOY.md)，要看参数请看 [README.md](README.md)。

---

## 先说背景：为什么需要中转

你手里有一条机场给的**订阅链接**。但直接用它会遇到三个麻烦：

1. **软件挑食。** Clash 要一种格式，v2rayN 要另一种，sing-box 又要第三种。同一个机场链接喂进去，出来的东西不一样。
2. **机场看人下菜碟。** 它会根据"你是谁"（请求里的 User-Agent）决定给你什么内容。有的机场给浏览器返回一份**空壳**——配置里一个节点都没有。而网页是改不了 User-Agent 的。
3. **网页打不开别人的网站。** 就算你找到一份能用的配置，想在浏览器网页里直接读机场的链接，会被 CORS 挡掉（机场服务器不允许网页读它的内容）。

中转服务解决的是第 2 和第 3 条：**它替你去取（能自定义 User-Agent），然后以允许网页读取的方式交给你。**

> 一个常见误解：中转**不是为了换 IP 地址**。国内线路、海外线路都能直连大多数机场。它真正的价值是"能设 User-Agent + 能剥掉 CORS"。

---

## 功能一览（更新后）

### 1. 格式互转

打开网页，把你的订阅链接粘进去（或者填进去让它在线取），选一个目标软件，点一下就生成配置文件。

- **能读**：Base64 节点列表、Clash 配置、sing-box 配置、Surge 配置、Quantumult X 配置，以及各类节点协议地址（完整清单见 [README「支持的协议」](README.md#支持的协议)）
- **能写**：Clash / Clash Meta、sing-box、Surge、Quantumult X

节点会自动按名字里的地区归组（名字里有"港""HK"进香港组，有"美""US"进美国组），不用一个个手挑。

### 2. 生成一条"永久"订阅链接

点一下生成新的订阅链接，得到形如：

```
https://你的域名/sub/一长串随机字符
```

把这条填进手机或电脑的科学上网软件。以后**机场改了节点、加了新线路，软件自己就能更新**，你不需要重新导入。

如果生成链接时选择了“地址:端口 / IP:端口 / 仅端口/复用”，这个标注模式也会被保存进中转链接；客户端每次刷新 `/sub/<id>` 时，节点名会自动带上 `复用N` 或 `独占`，不用再回网页手动生成配置。

> **删减边界**：网页上的手动删除、关键词筛选、协议/端口/正则规则和复用去重只作用于本机当前结果。它们不会上传到服务器，也不会进入已经创建的中转链接。删减后需要固定节点集合时，请导出“删减后订阅”或配置文件后直接导入客户端；需要持续更新时，请用原始订阅地址创建中转链接。

这条链接本身就是一个凭证，**别发到群里或公开地方**。

### 3. 缓存：不再反复敲机场的门 ✨新增

客户端一般每隔几分钟就来问一次"有新节点吗"。以前每次问，中转都跑去问一遍机场。10 台设备同时用，机场一分钟被敲十几次。

现在：**第一次去问，之后 15 分钟内谁再来要，直接把手上这份给他。**

- 响应从 1 秒多降到几十毫秒
- 机场那边请求量大幅下降
- 就算机场限流，你也不会被盯上

### 4. 机场耍赖也能兜住 ✨新增

有些机场对某些软件只回空壳配置（`proxies: []`）。以前遇到这种就只能给你一份空白，软件里显示"没有节点"。

现在的处理顺序：

1. 先按你的软件身份去要
2. 给的东西不对，就换几种常见软件身份挨个再试一轮
3. 还是不对，就**拿着已经拿到的节点，自己动手做一份**你要的格式

意思是：**只要机场还肯吐出任何一份能解析的东西，你那边就不会是空白页。**

同时它也学会了区分——比如 v2rayN 要的就是"节点地址清单"那种格式，拿到清单是**正常**的，不算空壳，不会再误判、不会多此一举去转换。

> ⚠️ **第 3 步"自己动手做一份"有一个已知限制**
>
> 服务端内置的转换器比网页版能认的协议少，具体取决于上游给的是哪种形式：
>
> | 上游给的形式 | 转换器能保留的节点类型 |
> |---|---|
> | 节点地址清单（Base64 / 明文 URI） | `vless` `vmess` `trojan` `ss` `tuic` `wireguard` `hysteria2` |
> | Clash 配置 / sing-box 配置 | `vless` `vmess` `trojan` `ss` `tuic` `hysteria2` |
>
> 也就是说 `ssr`、`anytls`、旧版 `hysteria`（不带 2）**一定会被丢掉**；
> `wireguard` 只有在上游给 URI 清单时才留得住，给 Clash 配置就留不住；
> `hysteria2` 只认全写 `hysteria2://`，常见简写 `hy2://` 服务端不认（网页版认）。
>
> 这只影响"机场只给空壳、需要服务端现场重建"这一种场景（响应头
> `X-Subconv-Source: rebuilt`）。正常情况下服务端把机场原始响应直接透传给软件，
> **不经过这个转换器，不受影响**。
>
> 网页版前端解析器能认的协议比服务端转换器多（如 `ssr`、`anytls`、旧版 `hysteria`、
> `hy2://` 简写），两者不是同一套实现。如果你在网页里能看到某些节点、而中转链接里
> 少几个，基本就是这个原因。

### 5. 机场挂了，还有一份存货 ✨新增

如果机场服务器抽风连不上，不会给你返回错误。**之前成功拿到的那份会照常给你**，软件照样能连，只是暂时看不到新节点。

这份存货最多用 **24 小时**。超过一天实在太旧了就明确报错，免得你拿着一堆已经失效的节点一直连不上还找不到原因。

### 6. 订阅 Token 不再外泄 ✨新增

你的订阅链接里藏着账号凭证，**等同于密码**。以前有三处会把它暴露：

| 位置 | 以前 | 现在 |
|---|---|---|
| 服务器访问日志 | 整条链接原样记录 | 显示 `url=[redacted]` |
| 出错提示 | 有时把链接拼进错误消息 | 一律替换成 `[upstream]` |
| 用量查询接口 | 返回记录里带原始 URL | 该字段已剔除 |

### 7. 流量用量能显示

机场返回的 `Subscription-Userinfo`（已用流量 / 总量 / 到期时间）会原样透传给你的客户端，所以 Clash Verge 这类软件里能正常看到剩余流量。

---

## 诊断：它是怎么跟你解释自己干了什么的

每次响应都带两个头，遇到"节点没更新"之类的问题先看这两个：

| 响应头 | 含义 |
|---|---|
| `X-Subconv-Source: cache` | 用的缓存，**没打机场**（正常情况） |
| `X-Subconv-Source: live` | 实时打的机场 |
| `X-Subconv-Source: rebuilt` | 机场没给对格式，**自己重建的** |
| `X-Subconv-Source: stale` | 机场连不上，**用了之前的存货**（该留意） |
| `X-Subconv-Age: 346` | 这份内容多少秒前拿的 |

`X-Subconv-Nodes: 7` 表示这次给了 7 个节点——如果你确定机场有 20 个，这个数字对不上就是上游的问题，不是转换的问题。

`X-Subconv-Upstream-UA` 告诉你最终是**用哪个软件身份**取到的内容。如果你请求的是 v2rayN，这里却是 `clash-verge/v1.3.6`，说明走了降级链。

---

## 现在还不能做什么

诚实说清楚边界：

- ❌ **测速**。节点快不快是客户端的事，中转不参与。
- ✅ **基础分流规则**。网页生成 Clash 配置时可选启用常用服务、广告拦截、私有地址、中国域名和中国 IP 直连规则；非中国域名使用 `geolocation-!cn`，并显式指定 GeoSite 下载源。分流规则依赖客户端可用的 GeoSite 数据，客户端若缓存了旧数据，需要先更新或清理本地 GeoSite 缓存。
- ❌ **多上游聚合**。一次只处理一条订阅链接，不能把三家机场合成一份。

---

## 已知坑：宝塔面板会让订阅延迟更新

如果你的 Nginx 是**宝塔面板**装的，注意这一条：

宝塔的全局 `/www/server/nginx/conf/proxy.conf` 里带着 `proxy_cache cache_one;`，但**没有写 `proxy_cache_valid`**。这种情况下 Nginx 会完全按后端给的 `Cache-Control` 来决定缓存多久。

如果中转回了 `public, max-age=840`，就等于命令 Nginx 把这条订阅链接缓存 14 分钟 —— **机场加了新节点，客户端最长 14 分钟看不到**。而中转链接本来就是每个客户端独享的，Nginx 这层缓存毫无收益。

两道都堵：

1. 后端对订阅响应一律回 `Cache-Control: no-store`，防重复交给上面第 3 条的应用层缓存
2. 每个 `proxy_pass` 后面显式加 `proxy_cache off;`

**不要改宝塔的 `proxy.conf`**，面板升级会覆盖你的改动。

验证有没有被 Nginx 偷偷缓存：

```bash
# 反复请求，观察 age 是否递增
curl -sI "https://你的域名/sub/<id>" | grep -i x-subconv
# 直接翻 Nginx 的缓存目录（宝塔是 levels=1:2，共三层）
strings /www/server/nginx/proxy_cache_dir/*/*/* 2>/dev/null | grep "/sub/"
```

---

## 回滚

中转有两个版本：`fetch_proxy.py`（v1，实时抓取无缓存）和 `fetch_proxy_v2.py`（v2，带上述全部功能）。

v2 通过 systemd drop-in 启用，**删掉那个文件就能回到 v1**，v1 原样保留：

```bash
rm -f /etc/systemd/system/subconv-fetch.service.d/v2.conf
systemctl daemon-reload && systemctl restart subconv-fetch
```
