# Security policy

Do **not** open public issues containing subscription URLs, relay IDs, API keys, access tokens, private keys, or server logs with credentials. Report a vulnerability privately through GitHub Security Advisories, or contact the repository owner through GitHub.

If a credential is exposed, revoke or rotate it immediately; deleting a commit does not invalidate a leaked secret.
